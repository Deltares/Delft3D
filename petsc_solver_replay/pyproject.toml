[project]
name = "petsc-solver-replay-benchmark"
version = "0.1.0"
description = "Benchmark PETSc solver and preconditioner configurations using replay dumps."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.8",
]

[tool.uv]
package = false